# ============================================================================
# Analyst Copilot Backend Dependencies
# ============================================================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# For dev: pip install -r requirements.txt -r requirements-dev.txt
# ============================================================================

# ── Core Framework ──────────────────────────────────────────────────────────
fastapi==0.104.1                 # Modern async web framework
uvicorn[standard]==0.24.0        # ASGI server with auto-reload
pydantic==2.5.0                  # Data validation (matches JSON schemas)
pydantic-settings==2.1.0         # Settings management from env vars

# ── Database ────────────────────────────────────────────────────────────────
sqlalchemy==2.0.23               # ORM for package versioning
alembic==1.12.1                  # Database migrations
psycopg2-binary==2.9.9           # PostgreSQL adapter
aiosqlite==0.19.0                # Async SQLite (dev/testing)

# ── LLM & RAG Infrastructure ────────────────────────────────────────────────
langchain==0.1.0                 # LLM orchestration framework
langchain-openai==0.0.2          # OpenAI integration
langchain-google-genai==0.0.5    # Google Gemini integration (alternative)
chromadb==0.4.18                 # Vector database for MITRE/MISP embeddings
sentence-transformers==2.2.2     # Generate embeddings
tiktoken==0.5.2                  # Token counting for OpenAI models

# ── External APIs ───────────────────────────────────────────────────────────
taxii2-client==2.3.0             # MITRE ATT&CK TAXII client
httpx==0.25.2                    # Async HTTP client for MISP API
python-dotenv==1.0.0             # Load .env files

# ── Utilities ───────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT tokens (future auth)
passlib[bcrypt]==1.7.4             # Password hashing (future auth)
python-multipart==0.0.6            # File upload support
orjson==3.9.10                     # Fast JSON serialization
pytz==2023.3                       # Timezone handling

# ── Monitoring & Logging ────────────────────────────────────────────────────
structlog==23.2.0                  # Structured logging
python-json-logger==2.0.7          # JSON log formatting
prometheus-client==0.19.0          # Metrics export (optional)
